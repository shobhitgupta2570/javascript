A decorator is essentially a function that takes a target object, a property key, and optionally a descriptor,
 and returns a modified version of them or replaces them altogether. 
They are often used to add annotations or modify behavior in a declarative manner.